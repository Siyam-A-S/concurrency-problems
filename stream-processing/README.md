# Stream Processing | C++

Implemented multi-threaded message broker with producer-consumer synchronization using mutexes and condition variables.

Designed thread-safe bounded queue and partition management ensuring concurrent producers/consumers access shared buffers safely, preventing race conditions with atomic operations.
